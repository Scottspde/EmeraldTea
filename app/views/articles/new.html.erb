<h1>New Article</h1>

<%= form_with scope: :article, url: articles_path, local: true do |form| %>

<% if @article.errors.any? %>
    <div>
        <h2>
            <%= pluralize(@article.arrors.count, 'errors') %> prohibited 
            this article from being saved:
        </h2>
        <ul>
            <% @article.erros.full_message.each do |msg| %>
            <li><%= msg %></li>
        </ul>
    </div>
    <% end %>

    <p>
        <%= form.label :title %><br>
        <%= form.text_field :title %>
    </p>

    <p>
        <%= form.label :text %><br>
        <%= form.text_area :text %>
    </p>
 
    <p>
        <%= form.submit %>
    </p>

    <%= link_to  'Back', articles_path %>

<% end %>